import React, {Component} from 'react';
import {View, Text} from 'react-native';

export class Messagesystem extends Component {
  render() {
    return (
      <View>
        <Text style={{fontSize: 100}}>jbujc</Text>
      </View>
    );
  }
}

export default Messagesystem;
